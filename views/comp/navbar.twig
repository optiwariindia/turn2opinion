{# <a href="#" class="js-fh5co-nav-toggle fh5co-nav-toggle fh5co-nav-white"><i></i></a>
<nav class="fh5co-nav" role="navigation">
	<div class="top">
		<div class="container">
			<div class="row">
				<div class="col-xs-12 text-right">
					<a href='{{project.contact.phone.link}}' class="num">{{project.contact.phone.text}}</a>
					<ul class="fh5co-social">
						{% for social in project.socials %}
							<li>
								<a href="{{social.link}}">
									<i class="{{social.icon}}"></i>
								</a>
							</li>
						{% endfor %}
					</ul>
				</div>
			</div>
		</div>
	</div>
	<div class="top-menu">
		<div class="container">
			<div class="row">
				<div class="col-xs-1">
					<div id="fh5co-logo">
						<a href="/">Turn<span>2</span>Opinion</a>
					</div>
				</div>
				<div class="col-xs-11 text-right menu-1">
					<ul>
						{% for link in project.navbar %}
							<li {% if link.active is defined %}class="active"{% endif %} >
								<a href="{{link.target}}" {{(link.action is defined)?"onclick=" ~ link.action : ""}} {{(link.title is defined)?"title='"~ link.title ~"'":""}}>{{link.name}}</a>
							</li>
						{% endfor %}
					</ul>
				</div>
			</div>

		</div>
	</div>
</nav> #}



<div class="main-header">
	<div class="top-header d-m-flex d-none">
		<div class="container">
			<div class="row">
				<div class="col-md-12">
					<div class="header-social-media d-flex align-item-center justify-content-end">
						{# <a href='{{project.contact.phone.link}}' class="num">{{project.contact.phone.text}}</a> #}
						<ul class="fh5co-social">
							{% for social in project.socials %}
								<li>
									<a href="{{social.link}}">
										<i class="{{social.icon}}"></i>
									</a>
								</li>
							{% endfor %}
						</ul>
					</div>
				</div>
			</div>
		</div>
	</div>
<div class="bottom-header">
	<div class="container">
		<div class="row">
			<div class="col-xs-3 col-sm-3">
				<div class="logo-area">
					<div id="fh5co-logo">
						<a href="/">Turn<span>2</span>Opinion</a>
					</div>
				</div>
			</div>
			<div class="col-xs-9 col-sm-9">
				<div class="menu-area">
					<div class="mobile-trigger" style="display: none;">
						<div class="line line-1"></div>
						<div class="line line-2"></div>
						<div class="line line-3"></div>
					</div>
					<nav>
					<div class="close-menu" style="display: none;">+</div>
						<ul>
							{% for link in project.navbar %}
								<li {% if link.active is defined %} class="active" {% endif %}>
									<a href="{{link.target}}" {{(link.action is defined)?"onclick=" ~ link.action : ""}} {{(link.title is defined)?"title='"~ link.title ~"'":""}}>{{link.name}}</a>
								</li>
							{% endfor %}
						</ul>
					</nav>
				</div>
			</div>
		</div>
	</div>
</div>

</div>
